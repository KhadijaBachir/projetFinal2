import { useState } from "react";
import { useNavigate } from "react-router-dom";
import CategoriesList from "./CategoriesList"; // Chemin relatif correct
import CategoryDetails from "./CategoryDetails"; // Chemin relatif correct
import RouletteDesDefis from "../components/RouletteDesDefis"; // Chemin relatif correct
import DefiDuJour from "../components/DefiDuJour"; // Chemin relatif correct
import DefiSurprise from "../components/DefiSurprise"; // Chemin relatif correct

const ExplorerDefis = ({ userId }) => {

  return (
    <div className="container mt-4">
      <h2>Explorer les Défis</h2>

    
    </div>
  );
};

export default ExplorerDefis;